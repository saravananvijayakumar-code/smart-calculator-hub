{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd ../frontend && bun install && bun run build:vite"
  },
  "dependencies": {
    "encore.dev": "^1.51.6",
    "marked": "^15.0.7",
    "openai": "^4.90.0",
    "vitest": "^3.0.9"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}